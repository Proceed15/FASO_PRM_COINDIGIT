Instalação:

cd backend

cd ChatBotAPI

Rodar: python.exe -m pip install --upgrade pip

cd npl_chatbot

Rodar: pip install -r requirements.txt
Rodar: pip install -U pip setuptools wheel
Rodar: pip install -U spacy
Rodar: pip install spacy download en_core_web_sm OU python -m spacy download en_core_web_sm
Rodar: pip install textblob
Rodar: python -m textblob.download_corpora ou pip install textblob.download_corpora